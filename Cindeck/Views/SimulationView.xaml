<UserControl x:Class="Cindeck.Views.SimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cindeck.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <DockPanel LastChildFill="False">
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                
                <Label Content="ユニット" Grid.Row="0" Grid.Column="0"/>
                <ComboBox Grid.Row="0" Grid.Column="1" MinWidth="200" DisplayMemberPath="Name" ItemsSource="{Binding Units}" SelectedItem="{Binding SelectedUnit, Mode=TwoWay}" HorizontalAlignment="Left" />
                
                <Label Content="楽曲" Grid.Row="1" Grid.Column="0"/>
                
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
                    <ComboBox MinWidth="200" ItemsSource="{Binding Songs}" SelectedItem="{Binding SelectedSong, Mode=TwoWay}" DisplayMemberPath="Title" />
                    <Button Command="{Binding LoadSongsCommand}" Content="Wikiから取り込む"></Button>
                </StackPanel>
                
                <Label Content="難易度" Grid.Row="2" Grid.Column="0"/>
                <ComboBox  Grid.Row="2" Grid.Column="1" DisplayMemberPath="Difficulty"
                           MinWidth="200" HorizontalAlignment="Left"
                           ItemsSource="{Binding SongData}" SelectedItem="{Binding SelectedSongData, Mode=TwoWay}"/>
                <Label Content="Groove Burst"  Grid.Row="3" Grid.Column="0"/>
                <ComboBox  Grid.Row="3" Grid.Column="1" MinWidth="200" HorizontalAlignment="Left"/>
            </Grid>
            <StackPanel Margin="0,5,0,0" Orientation="Vertical" DockPanel.Dock="Top">
                <CheckBox Content="ゲスト" />
                <CheckBox Content="ルーム効果"/>
                <CheckBox Content="サポートメンバー" />
            </StackPanel>
        </DockPanel>
        
    </Grid>
</UserControl>
